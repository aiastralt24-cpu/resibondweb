import type { Metadata } from "next";import Link from "next/link";import { InternalPageHero } from "@/components/internal-page-hero";import { TechnicalDocumentDirectory } from "@/components/technical-document-directory";import { products } from "@/lib/catalog";
export const metadata:Metadata={title:"Technical Data Sheets",description:"Access available official Resibond product technical data sheets.",alternates:{canonical:"/technical-data-sheets"}};
export default function Page(){const available=products.filter((product)=>product.tdsUrl).length;return <><InternalPageHero eyebrow="Technical library" title="Product data sheets." description="Find available Resibond technical data sheets by product." /><section className="section-shell"><aside className="document-scope-note" aria-label="Technical document availability"><strong>Document scope</strong><p>This library contains available TDS documents. Contact the technical team for SDS or certification requests.</p><Link href="/contact?source=technical-library">Request a document →</Link></aside><TechnicalDocumentDirectory products={products}/><p className="directory-note">{available} of {products.length} product TDS documents are available.</p></section></>}
