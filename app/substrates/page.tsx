import type { Metadata } from "next";import Link from "next/link";import { substrates,products,slugify } from "@/lib/catalog";
export const metadata:Metadata={title:"Sealants by Substrate",description:"Find Resibond products associated with glass, aluminium, mirror, metal, wood, UPVC and other substrates.",alternates:{canonical:"/substrates"}};
export default function Page(){return <><header className="page-hero"><span className="section-index">Substrate directory</span><h1>Find a compatible product route by surface.</h1><p>Use this directory as a starting point, then confirm compatibility against the product TDS.</p></header><section className="section-shell"><div className="link-directory">{substrates.map(item=><div key={item}><h2><Link href={`/substrates/${slugify(item)}`}>{item}</Link></h2>{products.filter(p=>p.substrates.includes(item)).map(p=><Link key={p.slug} href={`/products/${p.slug}`}>{p.name} →</Link>)}</div>)}</div></section></>}
