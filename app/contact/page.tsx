import type { Metadata } from "next";import { products } from "@/lib/catalog";import { EnquiryForm } from "@/components/enquiry-form";
export const metadata:Metadata={title:"Contact & Product Enquiry",description:"Contact Resibond for product selection, dealer, project or technical enquiries.",alternates:{canonical:"/contact"}};
export default async function Page({searchParams}:{searchParams:Promise<{product?:string;source?:string}>}){const query=await searchParams;return <><header className="page-hero compact-page-hero"><span className="section-index">Contact Resibond</span><h1>Tell us about the application.</h1><p>Share the surface, exposure and project context so the enquiry reaches the right product conversation.</p></header><section className="content-page"><div><span className="section-index">Enquiry form</span><h2>Project and product support.</h2><p>Your enquiry is saved securely for the Resibond team. Required fields are kept concise; product and project context helps us respond more usefully.</p></div><EnquiryForm products={products} product={query.product} source={query.source}/></section></>}
