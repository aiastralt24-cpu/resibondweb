import { NextRequest,NextResponse } from "next/server";import { adminCookie,createSession,verifyPassword } from "@/lib/admin-auth";import { allowRequest } from "@/lib/rate-limit";
export async function POST(request:NextRequest){const ip=request.headers.get("x-forwarded-for")?.split(",")[0]||"local";if(!allowRequest(`admin-login:${ip}`,5,15*60_000))return NextResponse.json({error:"Too many sign-in attempts. Try again later."},{status:429});const body=await request.json().catch(()=>null);const email=typeof body?.email==="string"?body.email.trim().toLowerCase():"";const expected=(process.env.ADMIN_EMAIL||"admin@resibond.in").toLowerCase();if(email!==expected||!verifyPassword(body?.password||""))return NextResponse.json({error:"Invalid email or password."},{status:401});const response=NextResponse.json({ok:true});response.cookies.set(adminCookie.name,createSession(email),adminCookie.options);return response;}
